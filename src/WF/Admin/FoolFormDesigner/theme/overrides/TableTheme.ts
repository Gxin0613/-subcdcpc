import { TableProps } from 'naive-ui';
type TableThemeOverrides = NonNullable<TableProps['themeOverrides']>;
const tableTheme: TableThemeOverrides = {
  borderColor: `rgba(198,196,192,0.6)`,
};

export default tableTheme;
